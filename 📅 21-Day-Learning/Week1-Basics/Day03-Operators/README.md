# Day 3: Operators (The Math) ➕➖

## 📚 Topics (Book: Tag 4)
-   Arithmetic (`+`, `-`, `*`, `/`)
-   Modulo (`%`) - The Remainder
-   Increment (`++`) and Decrement (`--`)

## 📝 Assignment
1.  Read **Tag 4** (first half).
2.  Open `MathOperators.cs`.
3.  Calculate the area of a rectangle.
4.  Experiment with division (what happens with `5 / 2` vs `5.0 / 2`?).

## 💡 Key Concept
**Integer Division**: If you divide two integers (`5 / 2`), C# throws away the decimal part (`2`, not `2.5`). Use `double` if you need precision!
